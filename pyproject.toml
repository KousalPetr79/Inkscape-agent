[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "inkscape-agent"
version = "0.1.0"
description = "Cross-platform agent foundation for controlling Inkscape"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
